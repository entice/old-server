# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

# Authentication
GET     /login                      controllers.Auth.loginGet
GET     /logout                     controllers.Auth.logoutGet
POST    /login                      controllers.Auth.loginPost
POST    /logout                     controllers.Auth.logoutPost

# Lobby
GET     /lobby                      controllers.Lobby.lobby

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public/assets", file)
